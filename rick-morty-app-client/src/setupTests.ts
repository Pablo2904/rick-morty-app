import "@testing-library/jest-dom";

// Mock SCSS and CSS moduless
// jest.mock("*.scss", () => ({}));

// // Mock react-router-dom
// jest.mock("react-router-dom", () => ({
//   ...jest.requireActual("react-router-dom"),
//   useNavigate: jest.fn(() => jest.fn()), // Mock `useNavigate`
// }));
